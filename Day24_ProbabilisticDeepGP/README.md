# Day 24 — Probabilistic Deep Learning & Gaussian Processes

## Overview
This day covers:
- Gaussian Process Regression (GPR) — predictive mean and uncertainty bands (±2 std).
- Gaussian Process Classification (GPC) — probabilistic decision surface (predictive probabilities).
- Probabilistic Deep Learning:
  - Preferred: TensorFlow Probability (TFP) Bayesian neural network demo (if `tensorflow` + `tensorflow-probability` installed).
  - Fallback: MC Dropout with Keras (if `tensorflow` installed).

