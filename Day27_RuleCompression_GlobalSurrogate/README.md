# Day 27 — Rule-based Model Compression & Global Surrogate Evaluation

## Overview
Extract human-readable rules from a surrogate decision tree learned to mimic a black-box (RandomForest). Compute per-rule coverage, precision, and fidelity vs black-box. Prune/compress rules using thresholds and evaluate rule-set coverage & fidelity across slices (subpopulations).

